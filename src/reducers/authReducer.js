

let myState = {
    user: {}
}

const authReducer = (state = myState, action) => {

      switch(action.type) {

        default: {
            return state
        }

      }

}


export default authReducer;